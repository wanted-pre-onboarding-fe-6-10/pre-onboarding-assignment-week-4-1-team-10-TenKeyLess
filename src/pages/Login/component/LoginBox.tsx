import LoginInput from './LoginInput';
import LoginTitle from './LoginTitle';

const LoginBox = () => {
  return (
    <div>
      <LoginTitle />
      <LoginInput />
    </div>
  );
};

export default LoginBox;
